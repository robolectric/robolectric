import org.robolectric.gradle.DeployedRoboJavaModulePlugin
import org.robolectric.gradle.RoboJavaModulePlugin

apply plugin: RoboJavaModulePlugin
apply plugin: DeployedRoboJavaModulePlugin

apply plugin: ShadowsPlugin

shadows {
    packageName "org.robolectric.shadows.gms"
    sdkCheckMode "OFF"
}

dependencies {
    compileOnly project(":shadows:framework")
    api project(":annotations")
    api libs.google.guava

    compileOnly libs.android.support.fragment
    compileOnly libs.google.gms.playService.base
    compileOnly libs.google.gms.playService.basement

    compileOnly AndroidSdk.MAX_SDK.coordinates

    testCompileOnly AndroidSdk.MAX_SDK.coordinates
    testCompileOnly libs.google.gms.playService.base
    testCompileOnly libs.google.gms.playService.basement

    testImplementation project(":robolectric")
    testImplementation libs.junit
    testImplementation libs.google.truth
    testImplementation libs.mockito.core
    testRuntimeOnly libs.android.support.fragment
    testRuntimeOnly libs.google.gms.playService.base
    testRuntimeOnly libs.google.gms.playService.basement

    testRuntimeOnly AndroidSdk.MAX_SDK.coordinates
}
