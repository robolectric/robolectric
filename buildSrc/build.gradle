apply plugin: "groovy"

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile "com.google.cloud:google-cloud-storage:0.8.0-beta"
}
